# Validator fixture snapshot: 0.8.0-draft.3

The `fixtures/` tree is a byte-for-byte copy of the fixture corpus associated with the immutable `v0.8.0-draft.3` specification snapshot. Validator package tests use this copy so later work in `spec/draft/fixtures/` cannot change Draft 3 coverage or expectations.