{
  "name": "@mcpdesc/specification",
  "version": "0.0.0-private",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "Development and validation tooling for the MCP Description specification",
  "type": "module",
  "scripts": {
    "test": "npm test --workspace @mcpdesc/validator && node scripts/validate-repository.mjs && node scripts/test-views.mjs",
    "validate": "node scripts/validate-repository.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "uri-template-matcher": "^1.1.2",
    "yaml": "^2.9.0"
  },
  "license": "Apache-2.0"
}
